package Amazon.OA2Review;

import java.util.*;

public class Syntax {

    public void solution() {

        LinkedList<String>  list = new LinkedList<>();
        List <Integer> ilist = new LinkedList<>();

        HashSet<Integer> set = new HashSet<>();

        HashMap<Integer, LinkedList> table = new HashMap<>();
        for (int i = 0; i < 10; i++) {
            LinkedList l = new LinkedList<Integer>();
            table.put(i, l);
        }

        Deque <Deque> doubleDD = new ArrayDeque<>();

        ArrayList<Integer> al = new ArrayList<>();

        Hashtable<Integer, HashMap> mTest = new Hashtable<>();

    }

}

